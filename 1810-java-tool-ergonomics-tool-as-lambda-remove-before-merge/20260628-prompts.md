### 14:35 Prompt

I am now in Phase 03.

In this phase we go through each open question and provide a **Resolution** section.

Starting with 3.1.

First sub-question:

> 1. Whether zero-arg tools need a dedicated overload.

Do the other languages even have zero-arg tools?

### 14:37 Response

I’ll check the SDKs for concrete zero-argument tool examples/usages so we can resolve 3.1 with evidence.

Search "DefineTool\(|fromObject\(|@CopilotTool|define_tool|tool\(" (dotnet, java, go, python, nodejs, … Timed out
Search "ReturnsOk\(\)|void_method\(\)|string_method\(\)|no args|no-arg|without arguments|arguments: \{… Timed out
Search "\(\) =>|func\(\) |def \w+\(\):|\|\|" (dotnet\test, java\src\test, go, python, nodejs, rust) 9 lines found

Read definetool.go 232 lines read
Read definetool_test.go 559 lines read
Read test_tools.py 429 lines read
Read tool.rs 1 line read

Search "arguments:\s*\{\}|\(\)\s*->|fn\s+\w+\s*\(\s*\)|NoArgs|empty args|zero" in *.rs (rust) 8 lines found
Search "define_tool\(|DefineTool\(|Tool::|ToolDefinition::|with_tools|tools:\s*\[" in *.rs (rust) 15 files found

Read manual_tool_resume.rs 152 lines read
Read tool.rs L1:260 (260 lines read)
Read tools.rs L1:220 (220 lines read)
Read tool.rs L260:460 (201 lines read)

Search "define_tool::<\(\)|struct\s+\w+\s*\{\s*\}|arguments\s*:\s*json!\(\{\}\)|\"type\":\s*\"obj… 35 lines found
Search "z\.object\(\{\}\)|type:\s*'object'\s*,\s*properties:\s*\{\}|arguments:\s*\{\}|tool\(|defi… 73 lines found

Read tools.py L1:260 (260 lines read)
Read CopilotToolTests.cs L130:170 (13 lines read)
Read CopilotToolTests.cs L14:55 (42 lines read)
Read tools.e2e.test.ts L88:125 (38 lines read)
Read tool_results.e2e.test.ts L165:200 (36 lines read)

Yes—several SDKs already support zero-arg (or zero-parameter) tools.

• .NET: true zero-arg delegate/method is used ( CopilotTool.DefineTool(ReturnsOk)  where  ReturnsOk()  has no args).
 dotnet/test/Unit/CopilotToolTests.cs:39,141 
• Python: explicit zero-param handler test ( def test_tool() -> str ) with empty invocation arguments.
 python/test_tools.py:107-127 
• Node.js/TypeScript: tools with empty parameter schema ( z.object({}) ) and no-arg handler ( handler: () => ... ).
 nodejs/test/e2e/tool_results.e2e.test.ts:176-180  (also  nodejs/test/e2e/tools.e2e.test.ts:100-103 )
• Go: zero-parameter tools are represented with an empty params type ( type Params struct{} ) and  {}  arguments.
 go/definetool_test.go:100-114 

So for 3.1, adding a dedicated zero-arg overload in Java would be consistent with cross-SDK usage patterns.

### 14:37 Prompt


So for this:

> 1. Whether zero-arg tools need a dedicated overload.

How would this signature **not** be the dedicated overload:

```java
ToolDefinition zeroArg = ToolDefinition.from(
        "zero_args",
        "Tool with zero args",
        () -> {
           return "OK";
        });
```

I don't recall what a lambda with no args looks like, but such a lambda is the last argument to this overload.

### 14:42 Response

It would be the dedicated zero-arg overload.

Your example is exactly the shape to add:

ToolDefinition.from(String name, String description, ToolFn0<R> handler)

(or  Supplier<R>  if you want JDK functional types).

For async parity, you’d likely also want:

ToolDefinition.from(String name, String description, AsyncToolFn0<R> handler)
// where handler returns CompletableFuture<R>

So yes: a no-arg lambda as the last parameter is the clean, dedicated zero-arg API.

### 14:42 Prompt

Why would I **not** want a Supplier<R>  for JDK functional types? Isn't this the most idiomatic way?

### 14:44 Response

You’re right— Supplier<R>  is the most idiomatic Java choice for sync zero-arg handlers.

You’d avoid it only if you want consistency with a custom tool-function family ( ToolFn0 ,  ToolFn1 , ...), or if you need behavior  Supplier  can’t express cleanly (for example checked exceptions, unless you wrap them, or a separate async SAM instead of  Supplier<CompletableFuture<R>> ).

So the practical recommendation is:

• Use  Supplier<R>  for sync zero-arg overload.
• Use a dedicated async SAM for async zero-arg overload (clearer than  Supplier<CompletableFuture<R>> ).

### 14:44 Prompt

Why is this tools-as-lambda tending to bifurcate into a dual "one for async, another for non-async" pattern?

### 14:45 Response

Because Java’s type system doesn’t give a clean single-SAM shape that is both ergonomic and unambiguous for:

•  R 
•  CompletableFuture<R> 

If you try one overload, you usually end up with awkward signatures ( Supplier<?> ,  Function<..., ?> ) and runtime branching on return type, or ambiguous lambda inference.

So APIs split into sync + async mostly for clarity and type inference:

• sync: simple lambda, returns  R 
• async: simple lambda, returns  CompletableFuture<R> 

It’s less about architecture and more about making call sites readable and compile-time-safe.

### 14:45 Prompt

Pardon my ignorance, but I am unfamiliar with this usage of the acronym SAM. Please make me smart.

### 14:47 Response

SAM means Single Abstract Method.

In Java, a lambda can be assigned to any interface that has exactly one abstract method (a “functional interface”), like  Supplier<T> ,  Function<T,R> ,  Runnable , etc.

Example:

Supplier<String> s = () -> "OK";   // lambda matches get()

 Supplier  is SAM because it has one abstract method:  T get() .

So when people say “SAM shape,” they mean “the functional-interface method signature the lambda must match.”

### 14:48 Prompt

You stated:

> • Use  Supplier<R>  for sync zero-arg overload.

So this would be 

```
ToolDefinition.from(String name, String description, Supplier<R> handler)
```

> • Use a dedicated async SAM for async zero-arg overload (clearer than  Supplier<CompletableFuture<R>> ).

How about

```
ToolDefinition.fromAysnc(String name, String description, Supplier<R> handler)
```

Would that meet the requirements in the zero-arg case?

### 14:53 Response

Almost—not as written.

If  fromAsync  takes  Supplier<R> , it’s still a sync signature unless you define  R  to be async. For zero-arg async, it should be one of:

ToolDefinition.fromAsync(String name, String description, Supplier<CompletableFuture<R>> handler)

or (clearer API surface):

ToolDefinition.fromAsync(String name, String description, AsyncSupplier<R> handler)
// interface AsyncSupplier<R> { CompletableFuture<R> get(); }

So  fromAsync(..., Supplier<R>)  only works if callers pass  R = CompletableFuture<T> , which is less explicit and easier to misuse.


### 14:53 Prompt


Where are you getting this `AsyncSupplier`? I see there is https://docs.oracle.com/en/java/javase/25/docs/api/java.base/java/util/function/Supplier.html but no corresponding async version.

### 14:54 Response


You’re correct—there is no JDK  AsyncSupplier .

I meant a custom interface you would define (if you choose that route), for example:

@FunctionalInterface
public interface AsyncSupplier<R> {
    CompletableFuture<R> get();
}

So your realistic choices are:

1.  fromAsync(..., Supplier<CompletableFuture<R>> handler)  using only JDK types, or  
2.  fromAsync(..., AsyncSupplier<R> handler)  with a tiny custom SAM for readability.


### 14:54 Prompt



Who coined this SAM acronym?

### 14:55 Response


There isn’t a single credited inventor.

SAM (“Single Abstract Method”) was community/OpenJDK terminology that became mainstream during Java 8 lambda design work (JSR 335), especially in materials from the Oracle/OpenJDK lambda expert group led by Brian Goetz.


### 14:56 Prompt

I have added `C:\Users\edburns\workareas\langchain4j-00-main`. This is the up-to-date LangChain4j. As you can see from ADR-005, our `CopilotTool` API is entirely based on their analogous tools API.

What do they do for tools-as-lambda in the case of zero-arg tools?

### 15:03 Response

I’ll inspect LangChain4j’s current tools API to see how they handle zero-arg tool lambdas, then answer with the exact pattern they use.

Search "ToolProvider|ToolExecutor|tool\(|@Tool|ToolSpecification|Function|Supplier|Runnable|execut… 7 files found
Search "zero|no args|no-arg|without arguments|parameters.*empty|\(\)\s*->" (~\workareas\langchain4… 6 lines found
Search "package dev\.langchain4j\.agent\.tool|class ToolSpecifications|class DefaultToolExecutor|… 13 files found

Search "**/langchain4j-core/src/main/java/dev/langchain4j/agent/tool/*.java" in ~\workareas\langc… 10 files found
Search "**/langchain4j-core/src/test/java/dev/langchain4j/agent/tool/*.java" in ~\workareas\langch… 3 files found

Search "toolProvider|toolExecutor|toolExecutors|ToolProvider|AiServices|tools\(|\.tools\(" (~\work… 8 files found
Search "\(\)\s*->\s*"|\(\)\s*->\s*\… rg: the literal "\n" is not allowed in a regex

Consider enabling multiline mode with the --multiline flag (or -U for short).
When multiline mode is enabled, new line characters can be matched.

Read tools.md L1:260 (260 lines read)
Read tools.md L260:620 (361 lines read)

Search "ToolProvider\.builder|ToolProvider\.from|toolExecutor|toolExecutors|Map<.*ToolExecutor|Sup… 8 lines found
Search "Methods without parameters are supported as well" (~\workareas\langchain4j-00-main\docs\doc… 1 line found

Read tools.md L1040:1165 (125 lines read)
Read ToolExecutor.java Path does not exist
Read DefaultToolExecutor.java 1 line read

Search "**/ToolExecutor.java" in ~\workareas\langchain4j-00-main 1 file found
Search "**/DefaultToolExecutor.java" in ~\workareas\langchain4j-00-main 1 file found
Search "**/AiServiceTool.java" in ~\workareas\langchain4j-00-main 1 file found

Read ToolExecutor.java 45 lines read
Read DefaultToolExecutor.java L1:260 (260 lines read)

Search "parameterCount\(|getParameterCount\(|parameters\(\)\.isEmpty|arguments\(\).+empty|invoke\("… 1 line found
Search "without parameters|no parameters|\(\)|required\(\)|properties\(\)|empty" (~\workareas\lang… 7 lines found

Read ToolSpecificationsTest.java L1:280 (280 lines read)

Search (grep)
C:\Users\edburns\workareas\langchain4j-00-main\docs\docs\tutorials\tools.md:1089:ToolExecutor toolExecutor = (toolExecutionRequest, memoryId) -> { 
C:\Users\edburns\workareas\langchain4j-00-main\docs\docs\tutorials\tools.md:1115:AiServiceTool tool = AiServiceTool.builder() 
C:\Users\edburns\workareas\langchain4j-00-main\docs\docs\tutorials\tools.md:1133:AiServiceTool bookingTool = AiServiceTool.builder() 
C:\Users\edburns\workareas\langchain4j-00-main\docs\docs\tutorials\tools.md:1139:AiServiceTool closeTool = AiServiceTool.builder() 
C:\Users\edburns\workareas\langchain4j-00-main\docs\docs\tutorials\tools.md:1145:AiServiceTool weatherTool = AiServiceTool.builder() 
C:\Users\edburns\workareas\langchain4j-00-main\langchain4j\src\main\java\dev\langchain4j\service\tool\ToolExecutionResult.java:42:    private final Supplier<String> resultTextSupplier; 
C:\Users\edburns\workareas\langchain4j-00-main\langchain4j\src\main\java\dev\langchain4j\service\tool\ToolExecutionResult.java:194:        private Supplier<String> resultTextSupplier; 
C:\Users\edburns\workareas\langchain4j-00-main\langchain4j\src\main\java\dev\langchain4j\service\tool\ToolExecutionResult.java:243:        public Builder resultTextSupplier(Supplier<String> resultTextSupplier) { 
C:\Users\edburns\workareas\langchain4j-00-main\langchain4j\src\main\java\dev\langchain4j\service\tool\ToolProviderResult.java:107:            tools.add(AiServiceTool.builder() 
C:\Users\edburns\workareas\langchain4j-00-main\langchain4j\src\main\java\dev\langchain4j\service\tool\ToolService.java:248:                result.add(AiServiceTool.builder()

In current LangChain4j, they don’t have a dedicated  Supplier -style “tool-as-lambda” zero-arg API.

What they do instead:

• High-level ( @Tool ): zero-arg tools are just methods with no parameters.
 docs/docs/tutorials/tools.md:377  (“Methods without parameters are supported as well.”)
• Programmatic/lambda path: the lambda is a  ToolExecutor , whose SAM is always
 execute(ToolExecutionRequest request, Object memoryId)  (not zero-arg).
 langchain4j/src/main/java/dev/langchain4j/service/tool/ToolExecutor.java:22 

So for a zero-arg tool, LangChain4j models “zero args” in the tool schema/method signature, but the executor lambda itself still receives request/context and can ignore them.

### 15:03 Prompt

Are there any usage examples of tools-as-lambda in the LangChain4j codebase?

### 15:04 Response

Yes—there are many examples, but they’re in the programmatic  ToolExecutor  style (lambda takes request/context), not  Supplier -style zero-arg lambdas.

Examples:

•  docs/docs/tutorials/tools.md:1089 
 ToolExecutor toolExecutor = (toolExecutionRequest, memoryId) -> { ... }; 
•  docs/docs/tutorials/skills.md:344 
 ToolExecutor validateOrderExecutor = (request, memoryId) -> { ... }; 
•  langchain4j/src/test/java/dev/langchain4j/service/tool/ProgrammaticCreatedImmediateReturnToolTest.java:30 
•  langchain4j/src/test/java/dev/langchain4j/service/tool/ToolExecutorTest.java:295 
•  langchain4j-skills/src/test/java/dev/langchain4j/skills/SkillTest.java:271 

    So: yes, tools-as-lambda exists, but via  ToolExecutor  ( (ToolExecutionRequest, memoryId) -> ... ), not a zero-arg lambda API.

### 15:10 Prompt

Looking at tools.md, I see some rendering directives like 

```
:::note
Not all LLMs support tools equally well.
The ability to understand, select, and correctly use tools depends heavily on the specific model and its capabilities.
Some models may not support tools at all, while others might require careful prompt engineering 
or additional system instructions.
:::
```

This tells me there should be some website that hosts the rendered docs.

Give me the URL to this `tools.md` in the official such website.

### 15:11 Response


The rendered page is here:

https://docs.langchain4j.dev/tutorials/tools


### 15:14 Prompt

But `tools.md` states:

> LangChain4j provides two levels of abstraction for using tools:
> - Low-level, using the `ChatModel` and `ToolSpecification` APIs
> - High-level, using [AI Services](/tutorials/ai-services) and `@Tool`-annotated Java methods

I don't see a mention of lambda in tools.md. 
